import { NotReadedPipe } from './not-readed.pipe';

describe('NotReadedPipe', () => {
  it('create an instance', () => {
    const pipe = new NotReadedPipe();
    expect(pipe).toBeTruthy();
  });
});
